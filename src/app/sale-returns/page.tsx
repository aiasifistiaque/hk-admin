'use client';

import { NextPage } from 'next';
import { ServerPage } from '@/components/library';

const page: NextPage = () => {
	return <ServerPage route='salereturns' />;
};

export default page;
